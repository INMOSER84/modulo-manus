<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="email_template_customer_approval" model="mail.template">
        <field name="name">Inmoser: Solicitud de Aprobación de Servicio</field>
        <field name="model_id" ref="model_inmoser_service_order"/>
        <field name="email_from">${object.company_id.email}</field>
        <field name="email_to">${object.partner_id.email}</field>
        <field name="subject">Solicitud de Aprobación - Orden de Servicio ${object.name}</field>
        <field name="body_html" type="html">
<div>
    <p>Estimado/a ${object.partner_id.name},</p>
    <p>Le informamos que se ha realizado el diagnóstico de su equipo <strong>${object.equipment_id.name}</strong>.</p>
    <p><strong>Diagnóstico:</strong> ${object.diagnosis or 'N/A'}</p>
    <p><strong>Monto Estimado:</strong> ${object.total_amount} ${object.currency_id.symbol}</p>
    <p>Por favor, acceda al portal para aprobar o rechazar el servicio.</p>
</div>
        </field>
    </record>
</odoo>
