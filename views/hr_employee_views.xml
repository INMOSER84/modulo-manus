<odoo>
    <!-- Vista de formulario de empleado (heredada) -->
    <record id="view_employee_form_inherit" model="ir.ui.view">
        <field name="name">hr.employee.form.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <field name="work_phone" position="after">
                <field name="is_technician"/>
                <field name="technician_code"/>
                <field name="specialization"/>
            </field>
            <notebook position="inside">
                <page string="Servicios Realizados">
                    <field name="service_order_ids">
                        <tree>
                            <field name="name"/>
                            <field name="client_id"/>
                            <field name="scheduled_date"/>
                            <field name="state"/>
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>

    <!-- Vista de árbol de empleado (heredada) -->
    <record id="view_employee_tree_inherit" model="ir.ui.view">
        <field name="name">hr.employee.tree.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_tree"/>
        <field name="arch" type="xml">
            <field name="work_phone" position="after">
                <field name="is_technician"/>
                <field name="technician_code"/>
            </field>
        </field>
    </record>

    <!-- Vista de búsqueda de empleado (heredada) -->
    <record id="view_employee_filter_inherit" model="ir.ui.view">
        <field name="name">hr.employee.filter.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_filter"/>
        <field name="arch" type="xml">
            <filter name="department" position="after">
                <filter name="technician" string="Técnicos" domain="[('is_technician', '=', True)]"/>
            </filter>
            <field name="name" position="after">
                <field name="technician_code"/>
                <field name="specialization"/>
            </field>
        </field>
    </record>
</odoo>
